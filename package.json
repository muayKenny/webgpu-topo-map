{
  "name": "webgpu-topo-map",
  "version": "1.0.0",
  "description": "WebGPU Topographic Map Renderer",
  "scripts": {
    "build:wasm": "cd wasm && wasm-pack build --target web",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.51",
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  },
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "geotiff": "^2.1.3"
  }
}
